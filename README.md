# REDACT.

A single-file, offline-first browser tool for photograph redaction and metadata stripping.

The application runs locally in the browser. Photographs are not uploaded by the app.

Open `index.html` locally or deploy the repository as a static site.
